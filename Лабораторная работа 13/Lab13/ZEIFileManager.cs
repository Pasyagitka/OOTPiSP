using System.IO;
using System.IO.Compression;

namespace Lab13
{
    class ZEIFileManager
    {

        //static public void CreateZIP(string dir)
        //{
        //    string zipName = @"G:\OneDrive\учёба\3-й_семестр\ООП\лр_13(потоки_файловая_cистема)\OOP_lab13\HDVInspect\HDVFiles.zip";
        //    if (new DirectoryInfo("..//..//..//..//ZEIFiles").GetFiles("*.zip").Length == 0)
        //    {
        //        ZipFile.CreateFromDirectory(dir, zipName);
        //        DirectoryInfo direct = new DirectoryInfo(dir);
        //        foreach (var innerFile in direct.GetFiles())
        //            innerFile.Delete();
        //        direct.Delete();
        //        ZipFile.ExtractToDirectory(zipName, dir);
        //    }
        //}
    }
}
